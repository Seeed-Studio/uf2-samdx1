name: Build
on:
  push:
    branches:
      - '**'
    tags:
      - '*'
  pull_request:
    branches:
      - '*'

jobs:
  crossbuild-xiao-bootloader:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v4
      - name: Git describe
        id: ghd
        uses: proudust/gh-describe@v2
      - name: Install toolchain
        run: |
          wget -qO- https://github.com/xpack-dev-tools/arm-none-eabi-gcc-xpack/releases/download/v8.3.1-1.4/xpack-arm-none-eabi-gcc-8.3.1-1.4-linux-x64.tar.gz | tar -zxv
          echo "xpack-arm-none-eabi-gcc-8.3.1-1.4/bin" >> $GITHUB_PATH
      - name: Compile
        run: make BOARD=XIAO_m0 UF2_VERSION_BASE=${{ steps.ghd.outputs.describe }}
      - name: upload github
        uses: actions/upload-artifact@v4
        with:
          path: |
            build/XIAO_m0/update-bootloader-XIAO_m0*.uf2
            build/XIAO_m0/update-bootloader-XIAO_m0*.bin
            build/XIAO_m0/bootloader-XIAO_m0*.bin
          name: update-bootloader-xiao

  crossbuild-dlp3-bootloader:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v4
      - name: Git describe
        id: ghd
        uses: proudust/gh-describe@v2
      - name: Install toolchain
        run: |
          wget -qO- https://github.com/xpack-dev-tools/arm-none-eabi-gcc-xpack/releases/download/v8.3.1-1.4/xpack-arm-none-eabi-gcc-8.3.1-1.4-linux-x64.tar.gz | tar -zxv
          echo "xpack-arm-none-eabi-gcc-8.3.1-1.4/bin" >> $GITHUB_PATH
      - name: Compile
        run: make BOARD=dlp3 UF2_VERSION_BASE=${{ steps.ghd.outputs.describe }}
      - name: upload github
        uses: actions/upload-artifact@v4
        with:
          path: |
            build/dlp3/update-bootloader-dlp3*.bin
            build/dlp3/bootloader-dlp3*.bin
          name: update-bootloader-dlp3


  crossbuild-nano33-bootloader:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v4
      - name: Git describe
        id: ghd
        uses: proudust/gh-describe@v2
      - name: Install toolchain
        run: |
          wget -qO- https://github.com/xpack-dev-tools/arm-none-eabi-gcc-xpack/releases/download/v8.3.1-1.4/xpack-arm-none-eabi-gcc-8.3.1-1.4-linux-x64.tar.gz | tar -zxv
          echo "xpack-arm-none-eabi-gcc-8.3.1-1.4/bin" >> $GITHUB_PATH
      - name: Compile
        run: make BOARD=arduino_nano33 UF2_VERSION_BASE=${{ steps.ghd.outputs.describe }}
      - name: upload github
        uses: actions/upload-artifact@v4
        with:
          path: |
            build/arduino_nano33/update-bootloader-arduino_nano33*.uf2
            build/arduino_nano33/update-bootloader-arduino_nano33*.bin
            build/arduino_nano33/bootloader-arduino_nano33*.bin
          name: update-bootloader-arduino_nano33

  crossbuild-spotlight-orin-bootloader:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v4
      - name: Git describe
        id: ghd
        uses: proudust/gh-describe@v2
      - name: Install toolchain
        run: |
          wget -qO- https://github.com/xpack-dev-tools/arm-none-eabi-gcc-xpack/releases/download/v8.3.1-1.4/xpack-arm-none-eabi-gcc-8.3.1-1.4-linux-x64.tar.gz | tar -zxv
          echo "xpack-arm-none-eabi-gcc-8.3.1-1.4/bin" >> $GITHUB_PATH
      - name: Compile
        run: make BOARD=spotlight_orin UF2_VERSION_BASE=${{ steps.ghd.outputs.describe }}
      - name: upload github
        uses: actions/upload-artifact@v4
        with:
          path: |
            build/spotlight_orin/update-bootloader-spotlight_orin*.bin
            build/spotlight_orin/bootloader-spotlight_orin*.bin
          name: update-bootloader-spotlight_orin


  upload-to-gcloud:
    runs-on: ubuntu-20.04
    if: github.event_name == 'push' && github.ref_type == 'tag'
    needs: [crossbuild-xiao-bootloader, crossbuild-nano33-bootloader, crossbuild-spotlight-orin-bootloader, crossbuild-dlp3-bootloader]
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true
      - run: ls -la 
      - id: auth
        name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GOOGLE_CREDENTIALS }}
      - id: upload-file
        uses: google-github-actions/upload-cloud-storage@v2
        with:
          path: ./
          destination: sl-mcu-firmware/bootloaders/
          glob: ./*bootloader-*
          process_gcloudignore: false
          gzip: false

